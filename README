This is a code snippet created for splendia candidates , applying to development positions. 
It's a basic class that needs to be unit tested, so the candidate should fork it and open a pull request with the new test classes covering the core class.